import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1E3A5F);

  //static const Color primary = Color(0xFF5C1F21);       // Bordô escuro
  static const Color background = Color(0xFFF5F4F2);    // Off-white
  static const Color textPrimary = Color(0xFF1A1A1A);   // Preto escuro
  static const Color textSecondary = Color(0xFF666666); // Cinza
  static const Color accent = Color(0xFF3A7CA5);        //Azul
  //static const Color accent = Color(0xFF3AA655);        // Verde para whatsApp não usado
}
